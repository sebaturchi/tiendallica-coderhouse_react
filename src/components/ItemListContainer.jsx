import React from 'react';

const ItemListContainer = ({ bienvenida }) => {
  return (
    <div className="container mt-4">
      <h2 className="text-center">{bienvenida}</h2>
    </div>
  );
};

export default ItemListContainer;
